name: Auto Merge

on:
  pull_request_target:
    types:
      - labeled

permissions:
  pull-requests: write
  contents: read

jobs:
  automerge:
    runs-on: ubuntu-latest
    steps:
      - uses: peter-evans/enable-pull-request-automerge@v2
        with:
          merge-method: squash
